---
- name: "Finalize: root password, SSH, unmount, reboot"
  hosts: target
  gather_facts: false

  tasks:
    # --- Relax password policy ---
    - name: Relax passwdqc policy
      ansible.builtin.lineinfile:
        path: "{{ mountpoint }}/etc/security/passwdqc.conf"
        regexp: '^enforce='
        line: 'enforce=none'

    # --- Root password ---
    - name: Set root password
      ansible.builtin.shell: >
        echo 'root:{{ default_root_password }}' | {{ chroot }} 'chpasswd'
      no_log: true
      changed_when: false

    # --- SSH ---
    - name: Install SSH server
      ansible.builtin.command: "{{ chroot }} 'emerge --noreplace net-misc/openssh'"
      register: ssh_emerge
      changed_when: "'Installing' in ssh_emerge.stdout"

    - name: Enable sshd
      ansible.builtin.command: "{{ chroot }} 'systemctl enable sshd'"
      register: sshd_enable
      changed_when: "'Created symlink' in (sshd_enable.stdout + sshd_enable.stderr)"

    - name: Allow root login via SSH (initial setup)
      ansible.builtin.lineinfile:
        path: "{{ mountpoint }}/etc/ssh/sshd_config"
        regexp: '^#?PermitRootLogin'
        line: 'PermitRootLogin yes'

    # --- Disable console beep ---
    - name: Disable console beep
      ansible.builtin.copy:
        dest: "{{ mountpoint }}/etc/modprobe.d/nobeep.conf"
        content: "blacklist pcspkr\n"
        mode: "0644"

    # --- Install vim ---
    - name: Install vim
      ansible.builtin.command: "{{ chroot }} 'emerge --noreplace app-editors/vim'"
      register: vim_install
      changed_when: "'Installing' in vim_install.stdout"

    # --- Set ZFS boot dataset ---
    - name: Set bootfs property
      ansible.builtin.command: zpool set bootfs=rpool/ROOT/gentoo rpool
      changed_when: false

    # --- Regenerate zpool.cache before reboot ---
    - name: Regenerate zpool.cache
      ansible.builtin.shell: |
        zpool set cachefile=/etc/zfs/zpool.cache rpool
        cp /etc/zfs/zpool.cache {{ mountpoint }}/etc/zfs/zpool.cache
      changed_when: false

    - name: Confirm reboot
      ansible.builtin.pause:
        prompt: "\n\e[1;32mInstallation complete.\e[0m Type \e[1;32myes\e[0m to reboot now, or anything else to skip"
      register: reboot_prompt

    # --- Unmount and reboot (only if user confirmed) ---
    - name: Unmount and export
      ansible.builtin.shell: |
        umount {{ mountpoint }}/dev/shm 2>/dev/null || true
        umount {{ mountpoint }}/dev/pts 2>/dev/null || true
        umount -R {{ mountpoint }}/dev 2>/dev/null || true
        umount -R {{ mountpoint }}/sys 2>/dev/null || true
        umount -R {{ mountpoint }}/run 2>/dev/null || true
        umount {{ mountpoint }}/proc 2>/dev/null || true
        umount {{ mountpoint }}/boot/efi 2>/dev/null || true
        swapoff -a 2>/dev/null || true
        zfs unmount -a 2>/dev/null || true
        zpool export -f rpool 2>/dev/null || true
      changed_when: false
      when: reboot_prompt.user_input == 'yes'

    - name: Reboot into new system
      ansible.builtin.shell: "nohup sh -c 'sleep 2 && echo b > /proc/sysrq-trigger' &"
      changed_when: false
      when: reboot_prompt.user_input == 'yes'
